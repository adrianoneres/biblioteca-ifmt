version: '3.1'

services:
  postgresql:
    container_name: postgresql
    image: postgres:14.2
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - 5433:5432
volumes:
  postgresql:
    external: true
